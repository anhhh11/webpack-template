describe('testem',function(){
  it('should work',function(){
    expect(1).toBe(2);
  });
});

describe('testem1',function(){
  it('should work',function(){
    expect(1).toBe(2);
  });
})


describe('Arithmetic', () => {
  it('should calculate 1 + 1 correctly', () => {
      const expectedResult = 2;

          expect(1 + 1).to.equal(expectedResult);
	    });
	    });
